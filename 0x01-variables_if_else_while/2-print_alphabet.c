#include <stdio.h>
/**
* main - prints the alphabet in lowercase...
* Return: Always 0.
*/
int main(void)
{
char aph;
for (aph = 'a'; aph <= 'z'; aph++)
{
putchar(aph);
}
putchar('\n');
return (0);
}
